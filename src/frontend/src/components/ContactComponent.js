import React, { Component } from 'react';

export default class ContactComponent extends Component {

    render() {
        return (
            <section>
                <div>Contact</div>
            </section>
        );
    }
}
